<%= form_for(opinion) do |f| %>
  <% if opinion.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(opinion.errors.count, "error") %> prohibited this opinion from being saved:</h2>

      <ul>
      <% opinion.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :autor %>
    <%= f.text_field :autor, required: true %>
  </div>

  <div class="field">
    <%= f.label :cuerpo %>
    <%= f.text_field :cuerpo, required: true %>
  </div>

  <div class="field">
    <%= f.number_field :post_id, hidden: true, value: params[:post_id] %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
